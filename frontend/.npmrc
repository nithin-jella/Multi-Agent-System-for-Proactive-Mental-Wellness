# Applied automatically by npm for every install/ci in this project.
#
# legacy-peer-deps is required because valtio@1.13.2 ships
# use-sync-external-store@1.2.0, which declares a peer dependency on
# React <=18. The project uses React 19, triggering a peer dep conflict
# that blocks npm ci without this flag.
legacy-peer-deps=true
